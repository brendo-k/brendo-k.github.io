import { defineConfig } from 'vite'

export default defineConfig({
  base: '/brendo-k.github.io/',  // Replace <REPO_NAME> with the actual name of your GitHub repo
  build: {
    outDir: 'dist'
  }
})